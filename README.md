How to use?
  - Put the project folder under the web server root as `MyWebSite`.
  - Start from `http://localhost/MyWebSite/index.php`.
  - Import `CreateProjectDB.sql` into MySQL before testing.
  - Staff login example: username `Alice Smith`, password `Password123!`.
  - Customer login example: username `Alice Chan`, password `alice123`.
